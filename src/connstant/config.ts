const configs = {
  APP_ENV: process.env.REACT_APP_ENV,
  API_DOMAIN: process.env.REACT_APP_API_DOMAIN,
  // API_DOMAIN: `${window.location.protocol}//${window.location.hostname}:${window.location.port}`,
};

export default configs;
